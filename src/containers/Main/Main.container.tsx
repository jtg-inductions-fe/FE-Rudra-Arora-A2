import { Box } from '@mui/material';

export const Main = () => (
    //  TODO: RA-A2-base-setup: fix this width, height and hardcoded colors
    <Box sx={{ background: '#D9D9D9', width: '100%', height: '100%' }}>
        Main Content
    </Box>
);
